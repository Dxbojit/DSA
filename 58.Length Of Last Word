class Solution {
    public int lengthOfLastWord(String s) {
        s=s.trim();
        if(s.length()==0)
        return -1;
        else if(s.indexOf(' ')==-1)
        return s.length();
        return (s.substring(s.lastIndexOf(' ')+1)).length();
    }
}
